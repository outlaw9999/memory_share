# .kit Cognitive OS Engine - Python Dependencies
# Target Runtime: Python 3.14.3
#
# Python >= 3.14 recommended for PEP 749 performance and generic typing semantics
# Python >= 3.11 required for legacy neural-memory watcher

# MCP server (Python 3.10+)
mcp[cli]>=1.0.0

# Live file watcher + semantic indexing (Python 3.11+ required)
neural-memory>=2.28.0
watchdog>=4.0.0

# v1.2.4-TITANIUM Core Dependencies
pyyaml>=6.0.0
ruff>=0.9.0
vulture>=2.10.0
